# ─────────────────────────────────────────────────────────────────
# TableReaderProj — Environment Configuration
# ─────────────────────────────────────────────────────────────────
# Copy this file to `.env` and fill in your values.
# The app reads these with python-dotenv (pip install python-dotenv).

# ── Person Names ────────────────────────────────────────────────
# Comma-separated list of people who share the credit card expenses.
# You can add as many as you want (e.g. PERSON1,PERSON2,PERSON3)
PEOPLE=Person1,Person2

# The "De quien" column label used for shared expenses
SHARED_LABEL=Los 2

# ── Mortgage (Hipoteca) ─────────────────────────────────────────
# Total monthly mortgage amount (in the same currency as transactions).
# E.g. if the full mortgage is 26000 and the split is 50%, each pays 13000.
MORTGAGE_TOTAL=26000

# ── Cost-sharing split ──────────────────────────────────────────
# Cost-split percentages (comma-separated, matches order of PEOPLE)
# Must add up to 100
SPLITS=50,50

# ── Google Sheets Table Reference ───────────────────────────────
# The name of the named range / table in Google Sheets that contains 
# the consolidated transactions. This is used to build the SUMIFS formulas.
TABLE_NAME=Transacciones_Banco_Enero_Febrero
